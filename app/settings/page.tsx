import { BellRing, Database, ShieldCheck, SlidersHorizontal } from "lucide-react";
import { WorkspaceLayout } from "@/components/shutterframe/workspace-layout";
import { PageHeader, Surface } from "@/components/shutterframe/workspace-ui";
const settings = [["Production guardrails", "Require an explicit approval before any production promotion.", ShieldCheck, true], ["Evidence retention", "Retain rehearsal logs and artifacts for 30 days.", Database, true], ["Run notifications", "Notify reviewers when a rehearsal is ready for approval.", BellRing, false], ["Default environment", "Use Production as the default review target.", SlidersHorizontal, true]] as const;
export default function SettingsPage() { return <WorkspaceLayout><div className="space-y-7"><PageHeader eyebrow="Workspace controls" title="Settings" description="Configure the defaults that keep your migration rehearsals predictable and reviewable." /><div className="grid gap-5 lg:grid-cols-[1.2fr_.8fr]"><Surface><div className="divide-y divide-[#e9eef3]">{settings.map(([title, text, Icon, enabled]) => <div key={title} className="flex gap-4 px-5 py-5"><span className="grid size-9 place-items-center rounded-lg bg-[#edf5fa] text-[#286078]"><Icon size={18} /></span><div className="min-w-0 flex-1"><h2 className="text-sm font-bold text-[#304056]">{title}</h2><p className="mt-1 text-sm leading-5 text-[#758195]">{text}</p></div><button type="button" aria-label={`${enabled ? "Disable" : "Enable"} ${title}`} className={`relative mt-1 h-6 w-11 rounded-full transition ${enabled ? "bg-[#26819a]" : "bg-[#d8e0e8]"}`}><span className={`absolute top-1 size-4 rounded-full bg-white shadow-sm transition ${enabled ? "left-6" : "left-1"}`} /></button></div>)}</div></Surface><Surface className="p-6"><p className="dashboard-kicker">Workspace identity</p><h2 className="mt-2 text-lg font-bold">Acme Corp</h2><p className="mt-2 text-sm leading-6 text-[#748196]">Production Team · us-east-1</p><button type="button" className="mt-8 w-full rounded-lg border border-[#dce5ed] py-2.5 text-sm font-bold text-[#3b4a60]">Manage team</button></Surface></div></div></WorkspaceLayout>; }
