# Database boundary

Rehearsal databases will be temporary Neon branches created through the Neon MCP
server by TrueForge. This Next.js application never receives a production
connection string and never performs branch cleanup itself.
