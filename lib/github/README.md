# GitHub boundary

TrueForge will use a GitHub MCP connection to read pull-request migration files.
The application layer only supplies a scoped request and displays the resulting
evidence; it does not run an alternate GitHub agent or persist a token in the
browser.
